import React from 'react';
import useTitle from '../../utilities/useTitle';

const About = () => {
    useTitle('About');
    
    return (
        <div>
            <h1>This is about</h1>
        </div>
    );
};

export default About;